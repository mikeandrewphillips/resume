# The COMPLETE CV - every role, bullet, all history and publications.
# Rendered with the classic multi-page LaTeX CV (not the one-page Deedy).
# No `tags:` key  ->  no filtering  ->  everything in resume.yaml.
label: Full CV
output: MichaelPhillips_FullCV
layout: classic
